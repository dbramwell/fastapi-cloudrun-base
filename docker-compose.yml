services:
  backend:
    build: ./backend
    volumes:
      - ./backend:/backend
    ports:
      - "8000:8000"
    working_dir: /backend
    command: fastapi dev src/main.py --host 0.0.0.0

volumes:
  dbdata: